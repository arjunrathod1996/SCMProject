# ============================================================
# This workflow is deprecated - use ci.yml instead
# Keeping for reference only - can be safely deleted
# ============================================================

name: SCM CI Pipeline (Deprecated - Use ci.yml)

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This workflow is deprecated. Please use ci.yml for CI/CD"
