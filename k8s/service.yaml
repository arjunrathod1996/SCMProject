apiVersion: v1
kind: Service
metadata:
  name: scm-app-service
  labels:
    app: scm-app

spec:
  type: ClusterIP

  selector:
    app: scm-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080